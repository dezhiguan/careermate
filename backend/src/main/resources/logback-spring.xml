<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

    <!-- 彩色控制台输出：时间(蓝绿)、级别(按级别配色)、线程(品红)、logger(青)、MDC(黄)、消息(默认) -->
    <property name="CONSOLE_LOG_PATTERN"
              value="%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr([%thread]){magenta} %clr(%-5level) %clr(%logger{36}){cyan} %clr(traceId=%X{traceId:-} requestId=%X{requestId:-} userId=%X{userId:-} sessionId=%X{sessionId:-}){yellow} %clr(-){faint} %msg%n"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <!-- 即使输出被重定向（非 TTY）也强制彩色，便于 IDE 控制台显示 -->
        <withJansi>false</withJansi>
    </appender>

    <springProfile name="json-logging">
        <property name="JSON_LOG_PATTERN"
                  value="%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} level=%level thread=%thread logger=%logger{36} traceId=%X{traceId:-} requestId=%X{requestId:-} userId=%X{userId:-} sessionId=%X{sessionId:-} msg=%msg%n"/>
        <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>${JSON_LOG_PATTERN}</pattern>
                <charset>UTF-8</charset>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="JSON_CONSOLE"/>
        </root>
    </springProfile>

    <springProfile name="!json-logging">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>
</configuration>
