careermate:
  debug:
    llm-api-enabled: false
  security:
    mode: ${SECURITY_MODE:single-user}
    # 演示/内测期跳过 API 鉴权，所有请求以 local-user 访问；恢复登录时设 SECURITY_DEV_SKIP_AUTH=false
    dev-skip-auth: ${SECURITY_DEV_SKIP_AUTH:true}

spring:
  output:
    ansi:
      enabled: ${SPRING_OUTPUT_ANSI_ENABLED:never} # 生产日志默认关闭彩色，避免文件/聚合器出现 ANSI 乱码
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
