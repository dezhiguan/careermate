# Local development (Vite proxy forwards /api → backend)
VITE_API_BASE_URL=/api
VITE_BASE_PATH=/
VITE_DEV_PORT=5174
VITE_API_PROXY_TARGET=http://localhost:8081

# Production build (careerforge.cn root path) — see deploy/env/careermate-frontend.env.example
