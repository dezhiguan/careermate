{
  "name": "careermate",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:e2e": "playwright test",
    "test:e2e:show": "playwright test --project=local-chrome-desktop --headed --workers=1",
    "test:e2e:show:mobile": "playwright test --project=local-chrome-mobile --headed --workers=1",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:auth": "playwright test tests/e2e/auth.spec.js --project=local-chrome-desktop --headed --workers=1"
  },
  "dependencies": {
    "vue": "^3.4.0",
    "vue-router": "^4.3.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.4.0"
  }
}
