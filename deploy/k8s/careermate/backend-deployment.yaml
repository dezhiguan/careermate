apiVersion: apps/v1
kind: Deployment
metadata:
  name: careermate-backend
  namespace: careermate
  labels:
    app: careermate-backend
spec:
  replicas: 2
  revisionHistoryLimit: 3
  minReadySeconds: 5
  strategy:
    type: RollingUpdate
    rollingUpdate:
      # Single-node k3s: avoid a third backend pod during deploy (prevents NodeNotReady/OOM).
      maxSurge: 0
      maxUnavailable: 1
  selector:
    matchLabels:
      app: careermate-backend
  template:
    metadata:
      labels:
        app: careermate-backend
    spec:
      containers:
        - name: backend
          image: careermate/backend:latest
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
          ports:
            - name: http
              containerPort: 18080
          envFrom:
            - secretRef:
                name: careermate-backend-env
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: prod,skywalking-log
            - name: JAVA_TOOL_OPTIONS
              value: >-
                -Xms512m -Xmx1280m
                -javaagent:/opt/skywalking-agent/skywalking-agent.jar
                -Dskywalking.agent.service_name=careermate-backend
                -Dskywalking.collector.backend_service=172.25.90.184:11800
            - name: SW_LOGGING_OUTPUT
              value: CONSOLE
          startupProbe:
            httpGet:
              path: /api/health
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 36
          readinessProbe:
            httpGet:
              path: /api/health
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /api/health
              port: http
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 1536Mi
          volumeMounts:
            - name: logs
              mountPath: /app/logs
            - name: skywalking-agent
              mountPath: /opt/skywalking-agent
              readOnly: true
      volumes:
        - name: logs
          hostPath:
            path: /opt/careermate/logs/k8s-backend
            type: DirectoryOrCreate
        - name: skywalking-agent
          hostPath:
            path: /opt/skywalking-agent
            type: Directory
